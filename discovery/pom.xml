<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>pingo</artifactId>
        <groupId>com.pingo.namnv</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <artifactId>discovery</artifactId>
    <packaging>jar</packaging>

    <properties>
        <lz4.version>1.8.0</lz4.version>
        <!-- Cap protoc THEO DUNG protobuf-java runtime (xem root pom.xml), khong dung protoc.version
             rieng cua vertx-grpc (3.21.12), tranh lech protoc-vs-runtime. -->
        <protoc.version>${protobuf.version}</protoc.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.pingo.namnv</groupId>
            <artifactId>commons-lang</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.pingo.namnv</groupId>
            <artifactId>event</artifactId>
            <version>1.0-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <artifactId>lz4-java</artifactId>
            <groupId>org.lz4</groupId>
            <version>${lz4.version}</version>
        </dependency>
        <dependency>
            <groupId>com.pingo.namnv</groupId>
            <artifactId>message</artifactId>
            <version>1.0-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>com.pingo.namnv</groupId>
            <artifactId>eventbus</artifactId>
            <version>1.0-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>

        <!-- Frame/FrameType/LinkGrpc sinh tu link.proto (harbor<->colony, xem ARCHITECTURE.md muc 12);
             chi can protobuf-java (runtime message) + grpc-stub/grpc-protobuf (class LinkGrpc sinh ra
             tham chieu MethodDescriptor/marshaller, du harbor/colony khong dung ImplBase/Stub cua no).
             KHONG can vertx-grpc-server/client o day, vi 2 cai do chi harbor (client)/colony (server)
             dung truc tiep, xem harbor/pom.xml, colony/pom.xml. -->
        <dependency>
            <groupId>com.google.protobuf</groupId>
            <artifactId>protobuf-java</artifactId>
        </dependency>
        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-protobuf</artifactId>
        </dependency>
        <dependency>
            <groupId>io.grpc</groupId>
            <artifactId>grpc-stub</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.tomcat</groupId>
            <artifactId>annotations-api</artifactId>
            <version>6.0.53</version>
            <scope>provided</scope>
            <!-- grpc-stub's generated @Generated annotation needs javax.annotation.Generated, removed
                 from java.base since JDK 9; ban toi thieu de compile, khong chay luc runtime. -->
        </dependency>
    </dependencies>

    <build>
        <extensions>
            <extension>
                <groupId>kr.motd.maven</groupId>
                <artifactId>os-maven-plugin</artifactId>
                <version>1.7.1</version>
            </extension>
        </extensions>
        <plugins>
            <plugin>
                <groupId>org.xolstice.maven.plugins</groupId>
                <artifactId>protobuf-maven-plugin</artifactId>
                <version>0.6.1</version>
                <configuration>
                    <protocArtifact>com.google.protobuf:protoc:${protoc.version}:exe:${os.detected.classifier}</protocArtifact>
                    <pluginId>grpc-java</pluginId>
                    <pluginArtifact>io.grpc:protoc-gen-grpc-java:${grpc.version}:exe:${os.detected.classifier}</pluginArtifact>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                            <goal>compile-custom</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
