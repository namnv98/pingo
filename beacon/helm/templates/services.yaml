apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app.kubernetes.io/monitor: "true"
spec:
  selector:
    app: {{.Chart.Name }}
  type: NodePort
  ports:
    - port: 8888
      targetPort: 8888
      nodePort: 31004