apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  labels:
    app.kubernetes.io/monitor: "true"
spec:
  selector:
    app: {{ .Chart.Name }}
  type: NodePort
  ports:
    - name: http
      port: 8090
      targetPort: 8090
      nodePort: 31007
